# Demo Killer Progress

## Current Phase

Phase 2: Benchmark & Trust Hardening.

## Completed

- Product direction locked: "Kill demos, turn them into production candidates."
- First wedge locked: AI-built Next.js SaaS / AI apps before launch.
- Public GitHub repo created: https://github.com/AVIDS2/demokiller
- MVP CLI implemented with local path and public GitHub URL inspection.
- Fixture-backed tests, source inspection, rule engine, JSON/Markdown reports, and recheck diff implemented.
- Real GitHub smoke tests succeeded against public AI SaaS repositories.
- Phase 2 plan written: `docs/superpowers/plans/2026-06-17-demo-killer-benchmark-trust-hardening-plan.md`.
- Archetype-aware benchmark manifest implemented.
- Benchmark runner, Markdown summary, CLI command, and npm script implemented.
- GitHub clone failures now produce user-facing errors and clean up temporary directories.
- Supported-scope guard added so unsupported projects return `Insufficient Evidence` instead of `Production Candidate`.
- Benchmark manifest expanded to `api-backend`, `admin-panel`, and `automation-worker`.
- Benchmark manifest expanded to `agent-app` and `content-site`.
- Report output now includes a phased hardening plan and recheck command.
- NPM package name unified as `demokiller`.
- NPM publish hardening added: package contract test, CLI shebang, prepack build, Node engine, LICENSE, clean tarball contents, and tarball install smoke test.
- Dev dependency audit cleaned by moving to Vitest 3.2.6 / Vite 6.4.3 while keeping Node 18+ support.
- `demokiller init` implemented as the first agent-native integration surface. It writes `.demokiller/AGENT.md` and a managed `AGENTS.md` production gate block.
- README rewritten as a Chinese-first product and user guide, with `README.en.md` for English users.
- README top section polished with a rendered banner, npm/download/license/CI/stars badges, centered navigation, and agent-facing product callout.

## In Progress

- Prepare the first public npm release and then add MCP as the next agent-native surface.

## Next Steps

1. Publish `demokiller@0.1.0` to npm after final confirmation.
2. Add the first MCP surface so agents can call Demo Killer as a production gate.
3. Improve hardening plan items from rule ids into concrete fix tasks.

## Known Risks

- Public repositories can change, disappear, or fail to clone; they must not be part of the default unit test suite.
- Current rules are intentionally narrow and focused on Next.js App Router TypeScript projects.
- Demo Killer must not claim "Production Ready"; it can only say "Production Candidate" within supported scope.
